[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "ASYNC"]
ignore = ["E501"] # 行の長さ制限だけは柔軟に（GUIコードは長くなりがちなため）

[tool.ruff.lint.isort]
combine-as-imports = true
force-single-line = false
